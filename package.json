{
  "name": "github-native-status-page",
  "version": "0.1.0",
  "description": "A static status page generated from GitHub Issues.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node src/cli.js build",
    "render": "node src/cli.js render",
    "sync-labels": "node src/cli.js sync-labels",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
